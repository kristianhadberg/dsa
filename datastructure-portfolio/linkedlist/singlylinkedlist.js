class Node {
  constructor(data, next) {
    this.data = data;
    this.next = next;
  }
}

export default class SinglyLinkedList {
  constructor() {
    this.head = null;
  }

  add(data) {}

  remove(data) {}

  getFirst() {}

  getLast() {}
}
