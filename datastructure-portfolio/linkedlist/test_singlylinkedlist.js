import SinglyLinkedList from "./singlylinkedlist.js";

const list = new SinglyLinkedList();

list.add(1);
// list.add(2);
// list.add(3);

// list.removeNode(firstNode.next.next);
// console.log(list.getFirst());
// list.remove(3);
// list.remove(2);
list.remove(1);
list.dumpList();
